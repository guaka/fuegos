# Proxy fallback snapshots
#
# Populated at GitHub Pages build time (`./data/fires.json`, `./data/firms.geojson`).
# The SPA tries `fuegos-proxy.crew.workers.dev` first, then these same-origin files
# when the Worker is blocked (some iPhone Lockdown / content-blocker setups).
