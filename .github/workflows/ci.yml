name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    name: Basic tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run test suite
        run: node ci/test.cjs
